# Create method `parrot` that outputs a given phrase and
# returns the phrase
def parrot(phrase='Squawk!')
  # puts returns nil
  puts phrase
  # if we want to return the phrase too, we need to explicitly return it
  return phrase
end
